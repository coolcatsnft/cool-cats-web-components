import React from 'react';
import { Label } from './Label';

export default {
  title: 'Form/Label',
  component: Label
}

export const Basic = (() => {
  return (
    <Label>A label component</Label>
  )
}).bind({});