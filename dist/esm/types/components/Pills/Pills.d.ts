/// <reference types="react" />
import { IPills } from '../../utils';
import './Pills.scss';
export declare function Pills({ children, className, bordered, states, ...rest }: IPills): JSX.Element;
export default Pills;
